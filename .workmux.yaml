agent: gemini
files:
  symlink:
  - .xsm-local
mode: window
panes:
- command: <agent>
  focus: true
- split: horizontal
- command: clear
  size: 5
  split: vertical
post_create:
- git submodule update --init --recursive
- mkdir -p .beads && printf '%s\n' "$WM_PROJECT_ROOT/.beads" > .beads/redirect
