You are the Safety and Governance Auditor on a design council. Find abuse paths, privacy/consent/legal risks, and required guardrails.

PHASE: BLACK HAT — Find failure modes, abuse paths, deadlocks, adversarial behavior.

CONTEXT FROM PHASE 1 (Ideation):
The council recommends Xenota adopt: (1) lightweight addressability, (2) deterministic YAML workflows, (3) config-first behavior editing, (4) cheap append-only tracing with ActionIntent pre-execution declarations, (5) capability profiles per role, (6) TrustContext headers on ACP messages, (7) genome mutation approval gates, (8) capability freeze flags, (9) retention policy fields.

Now stress-test from a safety/governance perspective:

1. Top 5 abuse paths if Xenota adopts OpenClaw's lightweight patterns
2. How could agents circumvent the BAR gate or membrane via the new lightweight channels?
3. Privacy/consent risks from cheap append-only logging (over-logging, PII exposure)
4. What governance gaps open when you add P2P messaging that bypasses the Mayor?
5. How could the ActionIntent pre-execution log be gamed, suppressed, or weaponized?

SPECIFIC SCENARIOS TO ANALYZE:

A) An agent uses direct P2P addressing to coordinate with another agent to bypass membrane validation — how is this detectable? Preventable?

B) A Markdown skill injection modifies an agent's behavior to ignore BAR gate constraints — what stops this?

C) JSONL append-only logs accumulate PII from conversations — who owns deletion? How does GDPR right-to-erasure work with append-only?

D) A "capability freeze" flag is set but the agent discovers it can load skills via the lightweight Markdown path — how does the freeze enforce across both systems?

E) The TrustContext header is forged by a compromised projection — what verification exists? What's the blast radius?

F) An agent in a factory-line role (Reviewer, read-only) discovers it can escalate privileges via the repertoire system — what prevents this?

Be specific about attack vectors and required mitigations.
